import styled from "styled-components";

export const Wrapper = styled.div`
  position: absolute;
  bottom: 0;
  left: 50%;
  transform: translateX(-50%);
  color: #828282;

  h4 {
    font-weight: 500;
    font-size: 1.4rem;
    line-height: 1.7rem;
  }

  a {
    color: inherit;
    font-weight: 700;
  }
`;
